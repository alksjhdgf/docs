# Report of Tests Using the ArkTS Interop Library

| Test Path / Category | Cangjie Call Lines | How It Uses the Library | Main Coverage |
| --- | ---: | --- | --- |
| `test/arkts_interop_testsuites/testsuite_basic_import` | 2 | Cangjie directly imports `ohos.ark_interop.*` and `ohos.ark_interop_macro.*`, while ArkTS imports functions from `libohos_app_cangjie_entry.so` | Low relevance: mainly validates import and call-path bring-up; basic import, ArkTS calling Cangjie functions, export of basic types and callback signatures |
| `test/arkts_interop_testsuites/testsuite_dynamic_load` | 0 | The Cangjie-side test engine directly imports `ohos.ark_interop.*` and works with a dependency subproject for dynamic loading | Low relevance: mainly validates the dynamic-loading path; dynamic loading of Cangjie modules, dependency-project participation in loading, runtime and load-path interaction |
| `test/arkts_interop_testsuites/testsuite_hybrid` | 303 | ArkTS uses `requireCJLib` from `libark_interop_loader.so`, and the Cangjie side uses interop registration/export mechanisms | `Array`, `ArrayBuffer`, `BigInt`, `Exception`, `External`, `Function`, `Object`, `Promise`, `register class / func / module / override`, path loading, thread mismatch, and range-related failures |
| `test/arkts_interop_testsuites/testsuite_idl` | 2 | ArkTS uses `libark_interop_api.so` and generated d.ts surfaces, while the Cangjie side exports interfaces through interop/IDL generation | Low relevance: mainly validates generated IDL surface usability; `async function`, `enum`, named parameters, `Option`, parameter count/type, return types, `JSArrayEx`, `JSMapEx`, `JSStringEx`, `BinaryTree`, `Zoo` |
| `test/arkts_interop_testsuites/testsuite_load_cangjie_so` | 5 | ArkTS repeatedly loads a Cangjie shared library through `requireCJLib` from `libark_interop_loader.so` | Loading a Cangjie so, repeated loading of the same so, persistence of global state |
| `test/arkts_interop_testsuites/testsuite_pure_cj` | 37 | Pure Cangjie-side tests directly depend on the interop library | `JSArray`, `JSArrayBuffer`, `JSBigInt` |
| `test/arkts_interop_testsuites/testsuite_pure_manual_interop` | 213 | The Cangjie side manually writes interop code directly against `ohos.ark_interop` APIs | `JSArray`, `JSArrayBuffer`, `JSBigInt`, `JSClass`, `JSError`, `Exception`, `External`, `IDLType`, `JSCallInfo`, `JSContext`, `JSValue`, `Object`, `Promise`, `String`, `Symbol`, `Utf16String` |
| `test/arkts_interop_testsuites/testsuite_reflect` | 0 | Reflection-related Cangjie tests run inside an interop test project | Low relevance: mainly tests reflection capability rather than direct interop API calls; `TypeInfo`, annotation lookup, constructor invocation, reflected members |
| `test/arkts_interop_testsuites/testsuite_reflection` | 0 | Reflection calls and boundary validations are executed inside the interop environment | Low relevance: mainly tests reflective invocation and boundary conditions rather than direct interop API calls; `getInstanceFunctions`, `getStaticFunctions`, generic-function `apply`, parameter-count errors, parameter-type errors, generic-argument-count errors, reflection exception messages |
| `test/arkts_interop_testsuites/testsuite_stringUtf16` | 65 | ArkTS uses `requireCJLib` to call Cangjie-exported UTF-16 handling logic | `compare`, `substr`, `substrAdvanced`, `split`, `splitAdv`, `indexOf`, `lastIndexOf`, `countOf`, `replace`, `contains`, `startsWith`, `endsWith` |
| `test/arkts_interop_testsuites/testsuite_threads` | 1 | The Cangjie-side test engine directly imports `ohos.ark_interop.*` | Low relevance: mainly tests thread scheduling and UIThread coordination; `spawn(UIThread)`, UI-thread binding, click-triggered cross-thread events, interop thread-scheduling coordination |
| `test/arkts_interop_testsuites/testsuite_worker_thread` | 7 | ArkTS uses `requireCJLib` from `libark_interop_loader.so`, including in worker-thread scenarios | Loading Cangjie modules inside a worker, page-to-worker message exchange, interop return values in worker scenarios |
| `CJInteropsTest / pure core capability`<br>`test/CJInteropsTest/entry/src/main/cangjie/src/pure` | 834 | Pure Cangjie-side tests directly import `ohos.ark_interop.*` and run low-level interop checks through `std.unittest` | `test_array.cj`, `test_arraybuffer.cj`, `test_bigint.cj`, `test_class.cj`, `test_error.cj`, `test_exception.cj`, `test_external.cj`, `test_idltype.cj`, `test_jscallinfo.cj`, `test_jscontext.cj`, `test_jstype.cj`, `test_jsvalue.cj`, `test_object.cj`, `test_prime.cj`, `test_promise.cj`, `test_string.cj`, `test_symbol.cj`, `test_utf16string.cj`, `test_require_ark_module.cj` |
| `CJInteropsTest / export and invocation`<br>`test/CJInteropsTest/entry/src/main/cangjie/src` + `test/CJInteropsTest/entry/src/ohosTest/ets/test` | 170 | The Cangjie side exposes values through `JSModule`, `JSContext`, and `JSValue`, while ArkTS validates the exported behavior through `libentry.so` or generated surfaces | `test_array_00/01/02`, `test_arraybuffer_00`, `test_bigint_00`, `test_exception_00`, `test_external_00`, `test_function_00`, `test_post_js_task`, `test_promise_00/01`, with ArkTS-side checks in `array_buffer_01.test.ets`, `function_01.test.ets`, `post_jstask_01.test.ets`, `promise_00/01.test.ets`, `test_big_int_00/01.test.ets`, `exception_01.test.ets`, and `test_external_00.test.ets` |
| `CJInteropsTest / registration mechanisms`<br>`test/CJInteropsTest/entry/src/main/cangjie/src` + `test/CJInteropsTest/entry/src/ohosTest/ets/test` | 50 | The Cangjie side exports behavior through interop-library registration APIs such as `JSModule.registerClass`, `registerFunc`, and `registerModule`, and ArkTS checks the registration result | `test_register_class.cj`, `test_register_func.cj`, `test_register_module.cj`, `test_register_override.cj`, `test_register_after_static_init.cj`, with ArkTS-side checks in `test_register_class.test.ets`, `test_register_func.test.ets`, `test_register_module.test.ets`, `test_register_override.test.ets`, and `test_register_after_static_init.test.ets` |
| `CJInteropsTest / runtime edge cases and type conversion`<br>`test/CJInteropsTest/entry/src/main/cangjie/src` + `test/CJInteropsTest/entry/src/ohosTest/ets/test` | 346 | Direct validation of `JSContext` runtime edge cases, cross-language mixed stack behavior, and type-value conversion | `test_newScope.cj`, `test_mixed_stack.cj`, `test_type_value_00.cj`, with ArkTS-side checks in `test_newScope.test.ets`, `test_mixed_stack.test.ets`, and `test_type_value_00.test.ets` |
